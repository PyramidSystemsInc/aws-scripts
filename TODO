
1. Get port flags working in Docker's native language in the `--container` flag of `launchEcsTask.sh`

2. Validate Docker Hub images exist using `docker search`

3. Create scripts to destroy resources

